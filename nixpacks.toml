# Nixpacks configuration for Railway full-stack deployment
# Deploys both backend (Express) and frontend (React/Vite) from a single service
# https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = [
  "cd backend && npm ci",
  "cd frontend && npm ci"
]

[phases.build]
# Set NODE_ENV to development temporarily so vite (a devDependency) is available
cmds = [
  "cd frontend && NODE_ENV=development npm install && VITE_API_URL=/api npm run build",
  "cd backend && DATABASE_URL=${DATABASE_URL:-postgresql://placeholder:5432/placeholder} npx prisma generate"
]

[start]
cmd = "cd backend && sh scripts/check-env.sh && sh scripts/deploy-db.sh && node src/server.js"
